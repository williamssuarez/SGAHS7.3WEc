<form method="post" action="{{ path('app_alergias_delete', {'id': alergia.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alergia.id) }}">
    <button class="btn">Delete</button>
</form>
