{% extends 'base.html.twig' %}

{% block title %}Ciudad{% endblock %}

{% block body %}
    <h1>Ciudad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ciudad.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ciudad.nombre }}</td>
            </tr>
            <tr>
                <th>Poblacion</th>
                <td>{{ ciudad.poblacion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ciudad_index') }}">back to list</a>

    <a href="{{ path('app_ciudad_edit', {'id': ciudad.id}) }}">edit</a>

    {{ include('ciudad/_delete_form.html.twig') }}
{% endblock %}
