{{ form_start(form) }}
    <div class="card-body">
        <div class="mb-3">
            {{form_label( form.nombre ) }}
            {{form_widget( form.nombre ) }}
        </div>
        <div class="mb-3">
            {{form_label( form.poblacion ) }}
            {{form_widget( form.poblacion ) }}
        </div>
    </div>

    <div class="card-footer">
        <button class="btn btn-success pull-left">{{ button_label|default('Guardar') }}</button>
    </div>
{{ form_end(form) }}
