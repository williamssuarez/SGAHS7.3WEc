<form method="post" action="{{ path('app_visita_hospitalaria_delete', {'id': visita_hospitalarium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visita_hospitalarium.id) }}">
    <button class="btn">Delete</button>
</form>
