{% extends 'base.html.twig' %}

{% block title %}Cama{% endblock %}

{% block body %}
    <h1>Cama</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cama.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ cama.codigo }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ cama.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cama_index') }}">back to list</a>

    <a href="{{ path('app_cama_edit', {'id': cama.id}) }}">edit</a>

    {{ include('cama/_delete_form.html.twig') }}
{% endblock %}
