<form method="post" action="{{ path('app_enfermedades_delete', {'id': enfermedade.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enfermedade.id) }}">
    <button class="btn">Delete</button>
</form>
