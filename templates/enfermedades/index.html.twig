{% extends 'base.html.twig' %}

{% block title %}Enfermedades index{% endblock %}

{% block body %}
    <h1>Enfermedades index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for enfermedade in enfermedades %}
            <tr>
                <td>{{ enfermedade.id }}</td>
                <td>{{ enfermedade.nombre }}</td>
                <td>{{ enfermedade.descripcion }}</td>
                <td>
                    <a href="{{ path('app_enfermedades_show', {'id': enfermedade.id}) }}">show</a>
                    <a href="{{ path('app_enfermedades_edit', {'id': enfermedade.id}) }}">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_enfermedades_new') }}">Create new</a>
{% endblock %}
