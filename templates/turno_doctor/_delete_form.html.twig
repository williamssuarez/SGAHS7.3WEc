<form method="post" action="{{ path('app_turno_doctor_delete', {'id': turno_doctor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ turno_doctor.id) }}">
    <button class="btn">Delete</button>
</form>
