<form method="post" action="{{ path('app_cirugia_delete', {'id': cirugium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cirugium.id) }}">
    <button class="btn">Delete</button>
</form>
