{% extends 'base.html.twig' %}

{% block title %}Medicamentos{% endblock %}

{% block body %}
    <h1>Medicamentos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicamento.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ medicamento.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ medicamento.descripcion }}</td>
            </tr>
            <tr>
                <th>Miligramos</th>
                <td>{{ medicamento.miligramos }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_medicamentos_index') }}">back to list</a>

    <a href="{{ path('app_medicamentos_edit', {'id': medicamento.id}) }}">edit</a>

    {{ include('medicamentos/_delete_form.html.twig') }}
{% endblock %}
