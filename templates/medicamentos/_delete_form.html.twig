<form method="post" action="{{ path('app_medicamentos_delete', {'id': medicamento.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medicamento.id) }}">
    <button class="btn">Delete</button>
</form>
