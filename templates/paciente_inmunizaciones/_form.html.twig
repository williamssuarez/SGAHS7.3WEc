{{ form_start(form) }}
{{ include('base/_form_error_messages.html.twig') }}
<div class="card-body">
    <div class="mb-3">
        {{form_label( form.inmunizacion ) }}
        {{form_widget( form.inmunizacion ) }}
    </div>
    <div class="mb-3">
        {{form_label( form.dosis ) }}
        {{form_widget( form.dosis ) }}
    </div>
    <div class="mb-3">
        {{form_label( form.fechaAplicacion ) }}
        {{form_widget( form.fechaAplicacion ) }}
    </div>
    <div class="mb-3">
        {{form_label( form.sitioAplicacion ) }}
        {{form_widget( form.sitioAplicacion ) }}
    </div>
    <div class="mb-3">
        {{form_label( form.fabricante ) }}
        {{form_widget( form.fabricante ) }}
    </div>
    <div class="mb-3">
        {{form_label( form.reacciones ) }}
        {{form_widget( form.reacciones ) }}
    </div>
</div>

<div class="card-footer">
    <div class="row">
        <div class="col-md-12">
            <div class="d-flex justify-content-between">
                {#<a class="btn btn-primary" href="{{ path('app_paciente_show', {'id': paciente.id}) }}">
                    <i class="bi bi-arrow-left"></i> Volver
                </a>#}

                <div class="d-flex">
                    <button class="btn btn-success">
                        {{ button_label|default('Guardar') }}
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}
