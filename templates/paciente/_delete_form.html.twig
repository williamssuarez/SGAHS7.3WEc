<form method="post" action="{{ path('app_paciente_delete', {'id': paciente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paciente.id) }}">
    <button class="btn">Delete</button>
</form>
