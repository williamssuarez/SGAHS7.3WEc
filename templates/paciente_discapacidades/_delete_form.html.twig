<form method="post" action="{{ path('app_paciente_discapacidades_delete', {'id': paciente_discapacidade.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paciente_discapacidade.id) }}">
    <button class="btn">Delete</button>
</form>
