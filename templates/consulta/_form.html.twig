{{ form_start(form) }}
{{ include('base/_form_error_messages.html.twig') }}
<div class="card-body">
    <div class="mb-3">
        {{form_label( form.tipoConsulta ) }}
        {{form_widget( form.tipoConsulta ) }}
    </div>
    <div class="mb-3">
        {{form_label( form.fechaInicio ) }}
        {{form_widget( form.fechaInicio ) }}
    </div>
    <div class="mb-3">
        {{form_label( form.paciente ) }}
        {{form_widget( form.paciente ) }}
    </div>
</div>

<div class="card-footer">
    <div class="row">
        <div class="col-md-12">
            <div class="d-flex justify-content-between">
                <a class="btn btn-primary" href="{{ path('app_consulta_index') }}">
                    <i class="bi bi-arrow-left"></i> Volver
                </a>

                <div class="d-flex">
                    <button class="btn btn-success">
                        {{ button_label|default('Guardar') }}
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}
