{# templates/emergencia/forms/_assign_bed_form.html.twig #}

{{ form_start(form, {'attr': {'id': 'assign-bed-form'}}) }}
<div class="mb-3 text-start">
    {{ form_label(form.camaActual, null, {'label_attr': {'class': 'fw-bold mb-2'}}) }}
    {{ form_widget(form.camaActual, {'attr': {'class': 'form-select srchSelect w-100'}}) }}
</div>

{#
IRONCLAD CSRF TOKEN:
Using the exact string generated by the Controller's TokenManager.
#}
<input type="hidden"
       name="{{ form._token.vars.full_name }}"
       value="{{ manual_csrf_token }}">

{{ form_end(form, {'render_rest': false}) }}
