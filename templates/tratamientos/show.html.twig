{% extends 'base.html.twig' %}

{% block title %}Tratamientos{% endblock %}

{% block body %}
    <h1>Tratamientos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tratamiento.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tratamiento.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tratamiento.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tratamientos_index') }}">back to list</a>

    <a href="{{ path('app_tratamientos_edit', {'id': tratamiento.id}) }}">edit</a>

    {{ include('tratamientos/_delete_form.html.twig') }}
{% endblock %}
