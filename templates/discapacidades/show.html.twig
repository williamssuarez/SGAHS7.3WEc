{% extends 'base.html.twig' %}

{% block title %}Discapacidades{% endblock %}

{% block body %}
    <h1>Discapacidades</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discapacidade.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ discapacidade.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ discapacidade.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_discapacidades_index') }}">back to list</a>

    <a href="{{ path('app_discapacidades_edit', {'id': discapacidade.id}) }}">edit</a>

    {{ include('discapacidades/_delete_form.html.twig') }}
{% endblock %}
